{% if article.tags.size > 0 %}
<li>
  <span class="fa fa-tags"></span><span>{{ 'blogs.article.tags' | t }}: </span>
  	{% for tag in article.tags %}
  		<a href="{{ blog.url }}/tagged/{{ tag | handle }}" class="tag">{{ tag | escape}}</a>
  	{% endfor %}
</li>
{% endif %}